defmodule ElixirBlogWeb.PostCommander do
  use Drab.Commander

  # defhandler remove_post(socket, sender) do
  #   IO.inspect(sender, label: "soememeemem")
  #   set_prop socket, "#output_div", innerHTML: "Clicked the button!"
  # end
end
